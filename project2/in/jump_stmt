#pragma source off
#pragma token off
#pragma statistic off

int main(){
    return (!((-((id12 + 1.21321) - 1e92) * true / "abccc" % false) < id[1][3][5][6][9] <= abc() == abc(1, 2, 3) >= 3 > 1 != 2) && 3) || 2;
    break;
    continue;
}
